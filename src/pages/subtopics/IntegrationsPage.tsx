import Settings from "@/pages/Settings";

/** Renders the existing Settings page (ManyChat, Google Cal, Voice, Webhook) */
const IntegrationsPage = () => (
  <div className="h-full p-6 overflow-auto">
    <Settings />
  </div>
);

export default IntegrationsPage;
